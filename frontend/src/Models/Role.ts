/**
 * Enum for user roles.
 * Role.User (2) – Regular user (can view and like vacations)
 * Role.Admin (1) – Admin user (can add, edit, delete vacations)
 */
export enum Role {
    Admin = 1,
    User = 2,
}
