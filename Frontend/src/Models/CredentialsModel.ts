/**
 * Model used during login for submitting credentials.
 * Used in login forms and userService.login().
 */
export class CredentialsModel {
    public email?: string;
    public password?: string;
}
